server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DevK-Jung/spring-cloud-config.git
          username: ${gitUsername}
          password: ${gitPassword}